# Expense-Tracker
A Flask-based expense management system with SQLite database.
It allows users to add, view, and delete expenses, set monthly and category-wise budgets, and visualize spending trends through interactive charts. The app also supports CSV export and displays income, expenses, and savings summaries in a clean dashboard.
